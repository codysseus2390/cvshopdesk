/** Bounds and default for TV Mode's screen rotation timer, in seconds. Shared by the
 * Tools -> TV Mode settings form, its server-side validation, and TV Mode itself. */
export const TV_ROTATION_SECONDS_MIN = 8;
export const TV_ROTATION_SECONDS_MAX = 300;
/** Unchanged until a shop explicitly sets its own value. */
export const TV_ROTATION_SECONDS_DEFAULT = 18;
